export const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";
export type Lead={id:number;full_name:string;email:string;phone?:string;company_name?:string;business_type?:string;enquiry:string;estimated_budget?:string;preferred_timeline?:string;ai_summary?:string;detected_service?:string;urgency?:string;budget_category?:string;lead_score?:number;lead_status?:string;recommended_action?:string;personalized_reply?:string;workflow_status:string;workflow_result?:string;follow_up_status:string;created_at:string;updated_at:string;error_message?:string};
export async function request<T>(path:string,options?:RequestInit):Promise<T>{const r=await fetch(API+path,{...options,headers:{"Content-Type":"application/json",...options?.headers},cache:"no-store"});if(!r.ok)throw new Error("Request failed ("+r.status+")");return r.status===204?undefined as T:r.json()}
